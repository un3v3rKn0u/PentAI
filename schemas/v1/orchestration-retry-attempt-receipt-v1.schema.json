{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.pentai.local/v1/orchestration-retry-attempt-receipt-v1.schema.json",
  "title": "PentAI OrchestrationRetryAttemptReceipt v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "attempt_id", "command_id", "command_digest", "assessment_id", "plan_id", "plan_revision", "task_id", "task_revision", "agent_id", "prior_attempt_id", "prior_attempt_digest", "failure_id", "failure_receipt_digest", "failure_class", "checkpoint_id", "checkpoint_digest", "lease_consumption_id", "worker_id", "worker_version", "lease_generation", "fencing_token", "recovery_generation", "capability_manifest_id", "manifest_revision", "budget_account_id", "budget_reservation_id", "budget_reservation_account_version", "budget_account_version", "approval_consumption_id", "policy_bundle_id", "policy_hash", "retry_policy_id", "retry_policy_revision", "retry_policy_digest", "eligibility_decision_id", "eligibility_decision_digest", "retry_budget_consumption_id", "retry_budget_consumption_digest", "attempt_number", "attempt_state", "earliest_retry_at", "purpose", "registered_at", "attempt_digest", "authority", "execution_enabled"],
  "properties": {
    "schema_version": {"const": "1.0.0"},
    "attempt_id": {"type": "string", "format": "uuid"},
    "command_id": {"type": "string", "format": "uuid"},
    "command_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "assessment_id": {"type": "string", "format": "uuid"},
    "plan_id": {"type": "string", "format": "uuid"},
    "plan_revision": {"type": "integer", "minimum": 3},
    "task_id": {"type": "string", "format": "uuid"},
    "task_revision": {"type": "integer", "minimum": 3},
    "agent_id": {"type": "string", "minLength": 1, "maxLength": 96},
    "prior_attempt_id": {"type": "string", "format": "uuid"},
    "prior_attempt_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "failure_id": {"type": "string", "format": "uuid"},
    "failure_receipt_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "failure_class": {"enum": ["checkpoint_stalled", "coordination_timeout", "runtime_unavailable", "worker_process_failed"]},
    "checkpoint_id": {"type": ["string", "null"], "format": "uuid"},
    "checkpoint_digest": {"type": ["string", "null"], "pattern": "^sha256:[a-f0-9]{64}$"},
    "lease_consumption_id": {"type": "string", "format": "uuid"},
    "worker_id": {"type": "string", "minLength": 1, "maxLength": 128},
    "worker_version": {"type": "integer", "minimum": 1},
    "lease_generation": {"type": "integer", "minimum": 1},
    "fencing_token": {"type": "integer", "minimum": 1},
    "recovery_generation": {"type": "integer", "minimum": 1},
    "capability_manifest_id": {"type": "string", "format": "uuid"},
    "manifest_revision": {"const": 1},
    "budget_account_id": {"type": "string", "format": "uuid"},
    "budget_reservation_id": {"type": "string", "format": "uuid"},
    "budget_reservation_account_version": {"type": "integer", "minimum": 2},
    "budget_account_version": {"type": "integer", "minimum": 3},
    "approval_consumption_id": {"type": ["string", "null"], "format": "uuid"},
    "policy_bundle_id": {"type": "string", "format": "uuid"},
    "policy_hash": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "retry_policy_id": {"type": "string", "format": "uuid"},
    "retry_policy_revision": {"const": 1},
    "retry_policy_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "eligibility_decision_id": {"type": "string", "format": "uuid"},
    "eligibility_decision_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "retry_budget_consumption_id": {"type": "string", "format": "uuid"},
    "retry_budget_consumption_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "attempt_number": {"const": 2},
    "attempt_state": {"const": "registered"},
    "earliest_retry_at": {"type": "string", "format": "date-time"},
    "purpose": {"const": "register_validation_retry_attempt"},
    "registered_at": {"type": "string", "format": "date-time"},
    "attempt_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "authority": {"const": "none"},
    "execution_enabled": {"const": false}
  }
}
