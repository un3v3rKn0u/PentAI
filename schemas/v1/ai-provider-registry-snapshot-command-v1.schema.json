{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.pentai.local/v1/ai-provider-registry-snapshot-command-v1.schema.json",
  "title": "PentAI AI Provider Registry Snapshot Command v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "command_id", "snapshot_id", "registry_id", "registry_revision", "registry_digest", "providers_digest", "requester", "authentication_context", "purpose", "requested_at", "expires_at", "production_enabled", "authority", "execution_enabled"],
  "properties": {
    "schema_version": {"const": "1.0.0"},
    "command_id": {"type": "string", "format": "uuid"},
    "snapshot_id": {"type": "string", "format": "uuid"},
    "registry_id": {"type": "string", "format": "uuid"},
    "registry_revision": {"type": "integer", "minimum": 1, "maximum": 2147483647},
    "registry_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "providers_digest": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "requester": {
      "type": "object",
      "additionalProperties": false,
      "required": ["actor_type", "actor_id", "session_id"],
      "properties": {
        "actor_type": {"const": "human"},
        "actor_id": {"enum": ["local-desktop-session", "test-session"]},
        "session_id": {"type": "string", "format": "uuid"}
      }
    },
    "authentication_context": {"const": "local_core_authenticated_session"},
    "purpose": {"const": "record_provider_registry_snapshot"},
    "requested_at": {"type": "string", "format": "date-time"},
    "expires_at": {"type": "string", "format": "date-time"},
    "production_enabled": {"const": false},
    "authority": {"const": "none"},
    "execution_enabled": {"const": false}
  }
}
